import { INode } from './types';
export declare class Utils {
    private static id;
    static createNode(name: string, parentId?: number, isLeaf?: boolean, data?: unknown, expanded?: boolean): INode;
    static compareObjects(a: any, b: any): boolean;
}
